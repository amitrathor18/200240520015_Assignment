1) Create a view that shows all of the customers who have the highest ratings.
 query:-
CREATE VIEW h_cust 
AS
SELECT * FROM custamer
WHERE rating= (SELECT max(rating) FROM custamer);

show indexes from custamer;

2) Create a view that shows the number of salespeople in each city.

 query:-
CREATE VIEW tns_city AS
SELECT city, count(city) "NO. OF SALES PEOPLE"
FROM salespeople
GROUP BY city;

show indexes from salespeople;

use information_schema ;

SELECT * FROM statistics;



3) Create a view that shows the average and total orders for each salesperson after
his or her name. Assume all names are unique.
 query:-
CREATE VIEW AV_TOT_SAL AS
SELECT sname,
       avg(onum) AVERAGE,
       count(onum) "TOTAL ORDERS"
FROM salespeople
JOIN orders using(snum)
GROUP BY sname;
 
 4) Create a view that shows each salesperson with multiple customers.
 query:-
CREATE VIEW m_c AS
SELECT sname,
       cname
FROM salespeople
JOIN customers USING (snum);
